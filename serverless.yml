service: pictarium

provider:
  name: aws
  runtime: nodejs6.10
  stage: v1
  region: ap-northeast-1

functions:
  app:
    handler: handler.app
    timeout: 30
    events:
      - http:
          path: /
          method: get

resources:
  Resources:
    S3BucketStorage:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: pictarium-storage
    S3BucketPhotos:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: pictarium-photos
        CorsConfiguration:
          CorsRules:
            - AllowedHeaders:
                - "*"
              AllowedMethods:
                - POST
                - GET
                - PUT
                - DELETE
                - HEAD
              AllowedOrigins:
                - "*"
